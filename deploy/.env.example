# Song Guessr 环境变量配置
# 复制此文件为 .env 并修改相应的值

# ===================
# 服务器配置
# ===================
NODE_ENV=production
PORT=3000
CORS_ORIGIN=*

# API 前缀（用于反向代理）
# 例如设置为 /api 后，所有路由会变成 /api/xxx
API_PREFIX=

# ===================
# MongoDB 配置
# ===================
MONGODB_URI=mongodb://songguessr:songguessr123@localhost:27017/songguessr?authSource=songguessr

# Docker Compose 使用的 MongoDB 凭据
MONGO_ROOT_USER=admin
MONGO_ROOT_PASSWORD=password123
MONGO_USER=songguessr
MONGO_PASSWORD=songguessr123

# ===================
# 管理员配置
# ===================
# 初始管理员账户（首次启动时创建）
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123

# ===================
# 客户端配置（前端使用）
# ===================
# WebSocket 服务器地址（生产环境填写实际域名）
VITE_WS_URL=ws://localhost:3000
VITE_API_URL=http://localhost:3000/api

# ===================
# 游戏配置
# ===================
# 默认游戏设置
DEFAULT_ROUNDS=5
DEFAULT_SONG_DURATION=15
DEFAULT_MAX_PLAYERS=8

# ===================
# 音乐 API 配置
# ===================
# 默认音乐服务器（netease, qq, kugou 等）
DEFAULT_MUSIC_SERVER=netease

# Meting API 地址（如使用自建 API）
# METING_API_URL=https://your-meting-api.com
